using UnityEngine;

[Icon("d_Skybox Icon")]
public class NewHBehaviour : hBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
